stages:
  - build
  - test

build-image:
  stage: build
  image: debian:latest
  script:
    - apt update
    - apt install -y build-essential gcc-mipsel-linux-gnu g++-multilib

unit-test-job:
  stage: test
  image: debian:latest
  script:
    - mkdir -p ~/tmp
    - ./test_all.py 3

