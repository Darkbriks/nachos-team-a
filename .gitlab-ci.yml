stages:
  - build
  - test

build-image:
  stage: build
  script:
    - yes |  apt install gcc-mipsel-linux-gnu || true
    - yes |  apt install g++-multilib || true
    - yes |  apt install build-essential || true

unit-test-job:
  stage: test
  script:
    - mkdir -p ~/tmp
    - ./test_all.py 3

